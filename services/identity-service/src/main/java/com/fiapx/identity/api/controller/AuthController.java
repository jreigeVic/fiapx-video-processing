package com.fiapx.identity.api.controller;

import org.springframework.web.bind.annotation.RestController;

/**
 * Controller scaffold. No endpoints are exposed in the scaffold per project rules.
 */
@RestController
public class AuthController {
    // Controller exists to satisfy package organization from LLD.
}

