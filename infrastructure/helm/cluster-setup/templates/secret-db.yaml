apiVersion: v1
kind: Secret
metadata:
  name: fiapx-db-credentials
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  DB_USER: {{ .Values.db.masterUser | quote }}
  DB_PASSWORD: {{ .Values.db.masterPassword | quote }}
