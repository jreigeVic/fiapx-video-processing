apiVersion: v2
name: microservice
description: Generic chart for a FIAP X Spring Boot microservice (deployed once per service as an independent release)
type: application
version: 0.1.0
appVersion: "1.0.0"
